import React from 'react';
import Layout from '../components/Layout';

const Policy = () => {
    return (
      <Layout title={"Amigo's Privacy & Policy"}>
        Policy
      </Layout>
    );
};

export default Policy;